# Galano Grotesque

Regular (400), Medium (500), SemiBold (600) and Bold (700) are copied from the existing local Intratone theme.

The following files are already declared in `theme.json`. Add them here when available:

- `GalanoGrotesque-Light.woff2` — normal, weight 300.
- `GalanoGrotesque-Italic.woff2` — italic, weight 400.

Until then, these two faces use the font stack's browser fallback. No configuration change will be needed when the files arrive.
